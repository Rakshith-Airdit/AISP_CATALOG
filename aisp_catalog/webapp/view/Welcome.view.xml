<mvc:View controllerName="com.catalog.aispcatalog.controller.Welcome"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:l="sap.ui.layout"
    xmlns:grid="sap.ui.layout.cssgrid"
    xmlns:core="sap.ui.core" core:require="{CurrencyType: 'sap/ui/model/type/Currency'}">

    <Page id="welcomePage" title="{i18n>title}" showHeader="false" enableScrolling="true">
        <content>
            <Panel height="100%">
                <headerToolbar>
                    <OverflowToolbar>
                        <Title text="{i18n>allProductsTitle}" level="H2" />
                        <ToolbarSpacer />
                        <Text text="Total: {oProductsModel>/products/length} products" visible="{= !!${oProductsModel>/products/length}}" class="sapUiTinyMarginEnd" />
                    </OverflowToolbar>
                </headerToolbar>

                <!-- Illustrated Message that takes full available height -->
                <VBox height="100%" justifyContent="Center" alignItems="Center" visible="{= !${oProductsModel>/products/length}}">
                    <IllustratedMessage id="noDataMessage" title="{i18n>noProductsAvailable}" description="{i18n>noProductsDescription}" illustrationType="sapIllus-NoData" illustrationSize="Auto" />
                </VBox>

                <!-- Products Grid -->
                <f:GridList id="productGrid" items="{oProductsModel>/products}" mode="None" width="100%" visible="{= !!${oProductsModel>/products/length}}" class="sapUiSmallMarginTop">
                    <f:customLayout>
                        <grid:GridBoxLayout boxMinWidth="18rem" />
                    </f:customLayout>

                    <f:GridListItem type="Active" press="onSelectProduct">
                        <f:Card class="sapUiContentPadding" height="100%">
                            <f:content>
                                <VBox class="sapUiSmallMargin" height="100%" width="100%">
                                    <Image src="{oProductsModel>ProductImage}" alt="{oProductsModel>ProductName}" width="100%" height="10rem" class="sapUiSmallMarginBottom" />
                                    <core:HTML content="&lt;hr style='border-top: 1px solid #e0e0e0;'&gt;" />
                                    <Title text="{oProductsModel>ProductName}" wrapping="true" class="sapUiSmallMarginBottom" />
                                    <!-- <ObjectStatus text="{oProductsModel>Category}" state="Success" inverted="true" class="sapUiTinyMarginBottom" /> -->
                                    <GenericTag text="{oProductsModel>Category}" design="StatusIconHidden" status="Success" class="sapUiSmallMarginBottom" />
                                    <Text text="{oProductsModel>ProductDescription}" wrapping="true" class="sapUiSmallMarginBottom" maxLines="3" />
                                    <HBox justifyContent="SpaceBetween" class="sapUiSmallMarginBottom">
                                        <Text text="Unit Price" />
                                        <ObjectNumber number="{path: 'oProductsModel>UnitPrice', type: 'sap.ui.model.type.Float', formatOptions: { minFractionDigits: 2, maxFractionDigits: 2 }}" unit="{oProductsModel>CurrencyCode}" state="Success" emphasized="true" />
                                    </HBox>
                                    <HBox justifyContent="SpaceBetween" class="sapUiSmallMarginBottom">
                                        <Text text="UOM" />
                                        <Text text="{oProductsModel>UnitOfMeasure}" />
                                    </HBox>
                                    <HBox justifyContent="SpaceBetween" class="sapUiSmallMarginBottom">
                                        <Text text="Code:" />
                                        <Text text="{oProductsModel>CommodityCode}" />
                                    </HBox>
                                    <HBox justifyContent="SpaceBetween">
                                        <Text text="Part #:" />
                                        <Text text="{oProductsModel>PartNumber}" />
                                    </HBox>
                                </VBox>
                            </f:content>
                        </f:Card>
                    </f:GridListItem>
                </f:GridList>
            </Panel>
        </content>
    </Page>
</mvc:View>